import ExtremumClient from "extremum-sdk";

const DEFAULT_URL = "https://auth.app-0.ajev84ud35k4m2lo4n39.y.extremum.io/";

const client = new ExtremumClient(DEFAULT_URL,{defaultAppId:"0"})

export default client
